/**
 * Claves de preferencias_usuario centralizadas — así quien lee (login,
 * AlertaDispositivoService) y quien escribe (PreferenciasController) nunca
 * divergen en el literal.
 */
export const CLAVE_ALERTA_DISPOSITIVO = 'seguridad.alertaNuevoDispositivo';
